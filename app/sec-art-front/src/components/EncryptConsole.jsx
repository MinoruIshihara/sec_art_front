import React from 'react';
import { Typography } from '@mui/material';

function EncryptConsole() {
  return (
    <div>
      <Typography variant="h6">Encrypt Console</Typography>
      {/* ここにファイルアップロード機能を実装 (バックエンドとの連携が必要) */}
    </div>
  );
}

export default EncryptConsole;